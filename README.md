# 张鑫的个人主页

This is a hand-edited static version of ``.

The original Framer export is preserved in `framer-original/` for reference. The editable site now uses normal files:

- `index.html` for the home page content
- `about/index.html` for the about page content
- `styles.css` for the visual design
- `script.js` for the contact note and scroll reveal interactions

## Run

```bash
npm start
```

Then open `http://localhost:3000`.
